
class HTTPHammer:

    def __init(self):
        pass

    @staticmethod
    def hit(url):
        print(f"Hitting '{url}' with HTTP")

        # start = Time.now
        # response = HTTParty.get(url)
        # finish = Time.now
        #
        # pp response.code
        # pp response.body
        #
        # response = {}
        # return response
